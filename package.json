{
  "name": "tanistagram.com",
  "private": true,
  "scripts": {
    "build": "npm run build:js && npm run build:css && npm run build:img && npm run build:copy && npm run build:webcomponents",
    "build:js": "gulp js",
    "build:css": "gulp css",
    "build:img": "gulp img & gulp cwebp && wait",
    "build:copy": "gulp copy",
    "build:webcomponents": "gulp webcomponents",
    "watch": "gulp watch",
    "deploy": "npm run build && node deploy.js"
  },
  "dependencies": {
    "dom-delegate": "^2.0.3",
    "grd": "^1.2.3",
    "normalize.css": "^4.0.0",
    "rx": "^4.1.0"
  },
  "devDependencies": {
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-plugin-transform-class-properties": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0",
    "browser-sync": "^2.11.1",
    "browserify": "^13.0.1",
    "gh-pages": "^0.11.0",
    "gulp": "^3.9.1",
    "gulp-csso": "^2.0.0",
    "gulp-cwebp": "^1.1.0",
    "gulp-uglify": "^1.5.3",
    "vinyl-buffer": "^1.0.0",
    "vinyl-source-stream": "^1.1.0"
  }
}
